import 'package:flutter/material.dart';

class AdminPublicacionesPage extends StatelessWidget {
  const AdminPublicacionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('📰 Publicaciones y Contenido'),
    );
  }
}
