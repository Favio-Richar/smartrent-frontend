import 'package:flutter/material.dart';

class AdminSuscripcionesPage extends StatelessWidget {
  const AdminSuscripcionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('💳 Administración de Suscripciones'),
    );
  }
}
