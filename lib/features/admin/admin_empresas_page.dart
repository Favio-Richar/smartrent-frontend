import 'package:flutter/material.dart';

class AdminEmpresasPage extends StatelessWidget {
  const AdminEmpresasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🏢 Gestión de Empresas'),
    );
  }
}
